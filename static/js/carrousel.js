document.addEventListener('DOMContentLoaded', function() {
    // Inicializar el carrusel
    var myCarousel = new bootstrap.Carousel(document.querySelector('#carouselId01'), {
        interval: 8000
    });
});
document.addEventListener('DOMContentLoaded', function() {
    // Inicializar el carrusel
    var myCarousel = new bootstrap.Carousel(document.querySelector('#carouselId02'), {
        interval: 8000
    });
});